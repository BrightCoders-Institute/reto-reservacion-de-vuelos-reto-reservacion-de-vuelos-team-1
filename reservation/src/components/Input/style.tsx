import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    backgroundColor: '#ffffff',
    padding: 10,
    borderRadius: 5,
    textAlign: 'center',
    borderWidth: 1,
    borderColor: '#B6B7BA',
  },
});
